{
  "name": "negroni",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "An open-source, agent-native advertising system for lead generation.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "setup": "npm --prefix app ci && npm --prefix web ci",
    "dev": "npm --prefix app run dev",
    "dev:app": "npm --prefix app run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm run build:app && npm run build:web",
    "build:app": "npm --prefix app run build",
    "build:web": "npm --prefix web run build",
    "test": "npm --prefix app test",
    "check": "npm --prefix app run check && npm --prefix web run typecheck",
    "validate": "npm --prefix app run validate && npm run build:web"
  }
}
