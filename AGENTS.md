# Lead Generation Intelligence Workspace

Keep the shared `lead-generation-ads-discovery-intelligence` skill canonical.
Do not duplicate or weaken its research method, evidence rules, platform
method, two-sided model, or delivery contract in the app.

Treat the PPL course archive, business-loan document, and MVA documents as
structural examples only. Never seed their markets, facts, people, competitors,
pricing, economics, geography, claims, legal conclusions, or copy.

Preserve the explicit boundary between the real Codex App Server adapter and
the deterministic synthetic fixture. Never silently fall back from real to
fixture execution. Hosted builds are fixture-only.

Keep external actions disabled. Do not add campaign actions, traffic, spend,
outreach, purchases, form submissions, calls, routing, publication, sharing
changes, or live mutations without an explicit approved product change.

Keep secrets out of browser state, JSON, prompts, logs, fixtures, and source
metadata. Maintain the localhost-only companion and scoped read-only permission
profile. Do not replace it with full-host access.

Run `npm run validate` and desktop/mobile visual QA before reporting a build
ready. Do not create `document-manifest.json` until native Google Docs have
been created, read back, and verified.
